# Architecture

Implementation data flow and module map for the token-budget pipeline.

- [[token-budget-pipeline]] — end-to-end pipeline from prompt capture to TUI rendering and source tracing.
- [[skill-visibility-store]] — atomic writes, snapshot isolation, shape validation, and auto-reload after save.
